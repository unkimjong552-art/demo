{
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "composer install --no-dev --optimize-autoloader --no-interaction && npm install && npm run build:railway && php artisan config:cache && php artisan event:cache && php artisan route:cache && php artisan view:cache"
  },
  "deploy": {
    "startCommand": "php artisan migrate --force --no-interaction && /start-container.sh",
    "healthcheckPath": "/up",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE"
  }
}
